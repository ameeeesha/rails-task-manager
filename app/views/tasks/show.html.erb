<h2>Description</h2>
<%= @task.details %>

<p>
  <%= link_to 'Edit Task', edit_task_path, class: 'btn btn-light my-3' %>
  <%= link_to 'Delete Task', task_path(@task), class: 'btn btn-light my-3', data: { turbo_method: :delete, turbo_confirm: "Delete #{@task.title}?" } %>
  <%= link_to 'Back to list', tasks_path, class: 'btn btn-light my-3' %>
</p>
